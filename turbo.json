{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputMode": "new-only"
    },
    "odk-web-forms#build": {
      "outputs": ["dist/**"]
    },
    "test": {
      "outputs": []
    }
  }
}
